# Groq — free tier, get a key at console.groq.com (takes 2 minutes)
GROQ_API_KEY=

# Anthropic — fallback, used if GROQ_API_KEY not set
ANTHROPIC_API_KEY=

# Admin mode — unlocks server-side key for interview demos
ADMIN_PASSWORD=
